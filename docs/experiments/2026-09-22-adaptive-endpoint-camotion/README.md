# Adaptive endpoint + canonical-derived Camotion

Offline: reuse the successful hybrid A′→B, cut each traversal at a **motion-alive editorial endpoint** after the destination is established, then apply the **stored canonical CameraMotionPlan** to that earlier rendered state. Production unchanged.

Does not regenerate A′→B. Does not replan from Be* / Ce* / De*. Does not shorten Kling generation — adaptive selection happens after the full clip exists.
