# C→D Camotion 50% strength test

Offline single-generation test. Reuses adaptive `Ce*` (frame 210 / 8.75 s) and the stored canonical C→D `startPlan`. Only change: `exposure.strength` 0.04 → 0.02. Production unchanged.
